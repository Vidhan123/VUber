// Testing using Jest (For reference)

// const { getToken } = require('../config/jwtConfig');

// test('Should get a token, () => {
//   const token1 = getToken({ email: 'test@test.com' });
//   expect(token1).not.toBe(null);
// });
